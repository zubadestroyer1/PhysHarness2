# Research effectiveness implementation

Approved user plan: restore the mathematical workbench, cooperative delegation, useful long-horizon memory, retrieval, computation, and honest reporting before two $25 regression attempts. Preserve mathematical autonomy and independent acceptance. Coordinator plans and audits; GPT-6 Sol implements. Existing dirty changes are prior authorized work and must be preserved. Do not commit private research data, credentials, or native archives. Do not launch paid calls until integration/preflight gates pass. Stop the dedicated VM after testing.

## Task 1: Research workbench, knowledge, and computation

Own workspace_tools.py, workspaces.py, new workbench/provider modules, research.py, knowledge/, science/, and dedicated tests. Do not edit research_worker.py, execution/responses.py, continuation.py, collaboration.py, memory.py, or shared existing tests without coordinating. Expose a registry factory usable by research_worker; send its exact interface to Task 2.

Implement isolated local Docker research workspaces through the existing provider contracts, targeting the dedicated Linux VM, never a host shell fallback. Pinned image/environment, no network/credentials/host socket mounts, bounded CPU/RAM/time/output, cleanup and checkpoint/restore. Read-only trusted library sources must come from the pinned image; writable scratch source is separate; hidden target proofs excluded. Supply source/declaration lookup, scratch Lean execution/diagnostics, generic scientific command/file tools, and exact rational polynomial/matrix checker wrappers. No computation result can confer proof status. Existing E2B must continue working. Local shell-only provider is not isolation.

Separate installed-library search from accepted-result search and attributed ideas. Remove required opaque lexical type_query filtering; retain backward compatibility with an optional explicitly named lexical filter, and make actual type query a separate explicit facility rather than mislabel token matching. Include environment/provenance/source references and reason codes for empty results. Return bounded readable UTF-8 text with hash/exact byte access. Scientific Python versions/seeds/precision/error semantics must be reportable; unknown error stays unknown.

Replace 64 KiB portable archives with backward-readable versioned CAS manifest + streamed files, 256 MiB configurable quota, explicit file/path/secret/cache policy and integrity checks. Essential state must persist before destruction; failure cannot silently drop workspace state. Do not copy arbitrary host files into worker. Add substantive contract/adversarial tests. Run Ruff and own tests. Report changed files, exact interface, actual tests, unsupported capabilities honestly.

## Task 2: Joined delegation, native continuation, integration

Own research_worker.py, continuation.py, collaboration.py, orchestration/workflows.py, temporal_delivery.py, relevant domain additions if needed, and dedicated tests. Integrate the registry from Task 1 and policies from Task 3 when ready. Coordinate shared files explicitly. No edits to acceptance boundary.

Joined child work defaults to returning to parent; detached explicit. Stable branch mailbox/reply_to_parent and typed return_result containing evidence status, artifact refs, unresolved obligations, execution failure. Cooperative ideas mode allows attributed findings without granting verification. Existing verified-only isolation remains supported. Scope delegated accounting to actual task lineage. Parent final response with joined work pending suspends/releases slot, coalesces completion events, resumes idempotently under fresh fence. Use compatible native continuation; portable fallback for incompatible runtime/model/tool/environment. Terminal handed_off sessions never revived. Child failures/cancellations delivered too. Preserve experiment budget across all descendants/continuations. No uncertainty replay of paid effects.

Expose execution status separately from proof/receipt status. Require workbench capability preflight for formal-research profile; do not silently launch deficient tools. Implement API/client/MCP surface additions after coordinating with root. Tests: one slot, parent ends early, nested children, detached branch, duplicates, child failure, parent recovery/crash, scope/privacy, no double accounting. Keep existing unit/Temporal contracts passing.

## Task 3: Context policy, stagnation, and compaction reporting

Own execution/responses.py, execution/parameters.py, execution/types.py as needed, new policy modules, memory.py only for readable artifact/working view improvements, and dedicated tests. Do not edit research_worker.py; send integration API to Task 2.

Research vs stress profiles: research compaction min(floor(.75 * configured context window), window - output reserve - 8192), giving 96000 for window128000/output16384. Explicit stress8192 remains available, never default research. Honor explicit caller overrides but validate safe bounds. Native ordinary continuation plus durable exact records; no mandatory mathematical method. Keep active source, last diagnostics, exact target/assumptions, dependencies, obligations and child findings readable and bounded. Provider opaque data stays private. Separate provider compaction item counters from boundary/pruning events, include terminal response and multiple items, no duplicate item count.

Detect repeated identical terminal reads (not pending result polling): warn at4 repeats, request bounded fresh recovery at8 without substantive intervening work; one recovery per unchanged progress state, repeated failure parks branch with typed status and coordinator visibility. Do not reset on mere duplicate notes/read tool args; preserve across restart/handoff with canonical state. Supply structured runtime signals to Task2 rather than bypassing lifecycle authority. Shared dollar/time budget persists. Optional cumulative token guard must be explicit and never reset with context. Tests crossing calls, terminal compaction, counter uniqueness, small windows, readable exact anchors, false positives, crash recovery, recovery-loop bound. No silent proof success.

## Task 4: Integration, independent audit, and qualification

Root coordinates review of each implementation and dispatches fixes to GPT-6 Sol, then independent cross-system audit. Preserve prior baseline snapshot and record observed test counts. Test larger checkpoints, interrupted uploads, malformed paths, resource admission, source corpus restrictions, failed native resume and accounting. Run full Python checks, actual Temporal/PostgreSQL tests where available, frontend checks for changed interfaces, formal scope/hash qualification as needed. New research worker image must include scientific packages pinned without exposing acceptance authority. Check memory headroom between research and verification jobs. No manufactured cloud/fleet qualification claims.

## Task 5: Two live regression attempts

Only after gates pass: fresh two known targets, $25 each, $50 aggregate, research compaction policy, cooperative attributed sharing, functional workbench, no hidden solution hints. Use new run directory/database/profile revision, not historical edits. Optional cumulative token ceiling disabled in favor of dollar/time envelopes for these tests. Monitor canonical usage/status and repeated work; separate scientific and runner outcomes. If fault, diagnose and fix but do not silently exceed aggregate budget through retries. Preserve authoritative artifacts/receipts and corrected historical metric interpretation. Shut down VM and monitor after testing. Report exact accepted results, cost, limitations and changes remaining.
