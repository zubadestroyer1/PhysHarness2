# Harder physics and research-loop hardening — 24 September 2026

The fresh post-fix retry completed cleanly with an independently verified proof. All six tasks/sessions completed, all workspaces were destroyed, the export passed integrity checks, and the dedicated VM and monitor are stopped. The earlier successful-but-faulted attempt remains explicitly recorded below.

## What changed

- **Timely, optional helpers.** Two seeded roots leave two of the four active slots for model-requested helpers. Task-specific tool lists and peer identifiers make delegation and valid messaging discoverable. The models choose decomposition and mathematical methods.
- **Durable continuation history.** Every continuation ordinal retains its exact source, successor, checkpoint, workspace and target bindings. Missing or stale links remain explicit errors, including after several handoffs.
- **Safe compaction recovery.** A crash between confirmed provider settlement and local context pruning can be recovered using exact settlement evidence without repeating the paid request. Uncertain external effects are still blocked.
- **Correctable workspace mistakes.** Pure command/path validation now runs before recording an external operation. Invalid arguments reach the model as correctable tool errors. Identical errors after an actual dispatch still require reconciliation. Relative-path instructions are explicit.
- **Stronger launch checks.** Fresh retries preserve old attempts, reconcile costs, bind the exact source amendment, enforce separate semantic review, and recheck current verifier qualification under the launch lock. They cannot silently reset the shared budget or import an earlier proof.

## Physics target and acceptance

The target is a finite network of coupled, forced nonlinear Duffing oscillators. It requires a uniform exponential energy estimate with explicit forcing constants under positive mass/damping, symmetric coercive stiffness, nonnegative quartic potential and explicit differentiable dynamics. It combines finite-sum identities, nonlinear energy differentiation, quantitative inequalities and differential comparison. It is a known-type development benchmark, not an open problem or novelty claim.

A complete private reference passed Comparator, Lean and Nanoda before paid work. Hidden-hole and altered-conclusion/definition controls were rejected. Models saw only the target and ordinary pinned library access; the evaluator's derivation and reference proof remained private. Target semantic review was performed by the assistant under user delegation; human expert publication review remains outstanding.

## Original live observations

| Attempt | Scientific outcome | Duration | Conservative model ledger |
|---|---|---|---|
| Single-root calibration | Unverified partial findings; no submitted candidate | 274.49 seconds | $1.470130 |
| Cooperative, before path fixes | Independently accepted complete target proof; supervisor blocked by two infrastructure faults | First accepted proof at 1,281.63 seconds (21m22s) | $41.733125 total |
| Fresh post-fix retry | Independently verified complete proof; clean supervisor completion | First proof 1,022.62 seconds (17m03s); final task 1,103.86 seconds (18m24s) | $49.102475 under amended $100 ceiling |

These are different-budget observations, not a controlled comparison. The calibration ended voluntarily despite remaining resources. The cooperative target required substantial proof development, but one run does not establish broad research difficulty or general long-horizon reliability.

The accepted proof is 26,418 bytes and is bound to the exact reviewed target. Receipt `64a21308-8c8a-4624-a82e-cc87354e3f92` records Comparator statement/dependency checks, Lean replay and independent Nanoda replay. Candidate SHA-256: `693bc59c88b187f1add909c20a1e05eabcd116f6e760826d092f5ae18b074e6d`. No publication or novelty approval follows from the independent-kernel tier label.

Two roots and four model-requested helpers actually started, at most four simultaneously. Real helper source was reused in the accepted proof: the derivative components and forced-term estimates were copied, and a finite-comparison lemma was adapted. The successful root passed through one actual native compaction, then submitted the accepted proof in its next context epoch. No live handoff occurred. Sixty messages include unreceived messages to a failed root; raw counts are not a measure of useful collaboration. The evidence shows reuse and continued work after compaction, not a causal performance advantage.

## Faults, recovery and evidence

One root used `cwd=/work` instead of the allowed workspace-relative `.`. Validation occurred before dispatch, but its exception incorrectly terminated the native session. A helper's absolute file-read path was validated only after a pending-operation marker, leaving an uncertain local read and held slot. These were harness error-boundary faults, not mathematical failures or RAM exhaustion. All 136 completed workspace runs reported zero OOM events.

The first native session remains explicitly abandoned historical uncertainty; it was not replayed or relabeled successful. The helper's failed read and zero-cost slot were retired through the operator API only after checking exact call/operation bindings, archive evidence and physical container absence. Its original operation result was null, so no original error record was invented. Final old-attempt ledgers have no reserved cost, active worker or uncertain operation. The cooperative export checks 2,676 unique artifacts. Export hashing establishes integrity; the separate trusted receipt establishes proof acceptance.

Root observed five failing pre-fix regressions and four passing controls, then passing path fixes. The complete core suite passed 1,191 tests, with 14 opt-in skips and 21 upstream warnings. A separate real-Docker test rejected bad paths without pending effects, allowed subsequent valid file/Lean work in the same workspace and cleaned up. Four-worker capacity, verifier overlap, cancellation, fifth-slot rejection and three exact archive/restore cycles were qualified locally. Final frozen-source test results and retry audit follow below.

## Limits and remaining gates

This work qualifies a small local configuration. It does not establish cloud failure recovery, 128-worker endurance, thousand-worker scaling, live repeated handoff, month-long scientific memory quality, or new physics discovery. The next scale step must be gradual and measured. Independently verified proof success and an operationally clean run are separate outcomes.

The initial round allowance was $52. The user subsequently authorized a $100 total ceiling for the running retry; the explicit amendment is recorded below. Calibration, faulted cooperative run and fresh retry together consumed $92.305730 of conservative ledger accounting. Including $43.762893 from earlier experiments, cumulative historical accounting is $136.068623. All failures count. Cached-token price estimates are not invoice reconciliation; no savings were silently added to an allocation.

## User-authorized budget increase

While the fresh retry was running, the user explicitly increased its ceiling to $100, including existing retry spending. Root raised both canonical ceilings atomically without changing spent/reserved balances or workers. The operation is idempotent, revision-checked, operator-scoped and recorded as `experiment.budget.amended`. The original launch manifest and prior cost history are preserved. Its $8.796745 launch allowance is historical; the current retry ceiling is $100. Earlier attempts total $86.966148 and remain separate. No higher fleet or additional-run budget is implied.

The final frozen main suite passed 1,194 tests, with 14 opt-in skips and 21 upstream warnings. Two additional tests passed for the subsequently added standalone operator budget-amendment helper, plus root's separate preservation/idempotency fixture. Core execution source remained frozen throughout the retry.

## Next scale qualification

Before a fleet claim, measure 8–16 active workers: root/helper queue waits, parent yielding, useful artifact consumption, messages to finished recipients, cancellation/restoration, verifier backlog and accounting contention. Current software has optional yielding and durable delivery, but those mechanisms are not proof of fair or beneficial behavior at higher scale. Perform a bounded real interruption/resume test with exact continuation lineage; this round's complete repeated-handoff evidence is deterministic plus VM archive/restore, not live model handoff.

A concrete controller constraint also needs a reviewed change before 128: `TeamRunManifest.max_concurrency` currently has an upper bound of 100. Increasing that number alone would not qualify a 128-worker platform. Managed recovery, sharding/fairness and the 72-hour endurance workload remain separate deployment/evaluation work. The budget increase used an audited standalone operator helper; promote that behavior into the public control interface with concurrency and authorization tests before routine fleet operations.

## Final retry evidence and shutdown

Receipt `9c834332-48e9-4e75-947e-9aaa3e5b13b4` binds problem `7881534e-d302-4fca-b155-04dbde443e8a`, review `3cdf086e-ec47-4300-9359-b6e270455f11`, target `c48d55b82a9fd454b3b8df3c5d76b45fb2869295b8ea1c4986309e36b4ef49e2`, the original Challenge SHA and pinned environment, and the submitted candidate `d54ff7f271170c2ec50440105e8828a4b3b45fa47b4b267139703c1293b272cf`. Comparator, Lean and Nanoda accepted the 27,321-byte candidate. The axiom list is the allowed policy upper bound, not an exact measured list of used axioms. `publication=true` requests that checker tier; expert publication approval remains false.

The accepted candidate includes 57/57, 24/24, 55/55 and 27/28 substantive normalized lines from four earlier helper artifacts. Exact source transfer and successful local compilation were also observed. Sixty direct messages and 47 acknowledged delivery batches accompanied the work. Six native sessions and six tasks completed; 315 workspace operations completed. All final checkpoints were checked. This retry had zero native compactions and zero handoffs, and therefore adds no live evidence for either feature. The original successful root's one native compaction remains the actual long-context observation in this round.

There were 185 completed workspace commands: 109 exit-zero, 72 exit-one development checks, and four exit-127 login-shell PATH errors. All recorded OOM deltas were zero; maximum observed worker memory peak was 1,044,377,600 bytes under its 2 GiB limit. Models received eight typed tool rejections and continued. These are included in the detailed evaluation; successful completion does not mean every tool call succeeded.

The retry used 338 settled native responses: 19,019,962 input tokens plus 155,248 output tokens, exactly reconciling the 19,175,210 ledger tokens. These are cumulative input/output counts, not a single context window. No response output-cap hits were observed. Conservative ledger usage was $49.102475; the separately calculated cached-token price estimate is $6.997658, not a confirmed invoice. No remaining allocation was spent merely to exhaust the budget.

A private export checks 3,246 unique artifacts, manifest SHA `81d1b15080d4fd7db1f13a266c8b5d4c2b4e9131bee942bbbe70aaffe9236221`. Hash checking and receipt validation were independently repeated. No transcript, proof package or code was published/pushed. The experiment is paused, with zero active workers, reserved cost/tokens, uncertain operations, open model reservations or nonterminal tasks. Root observed an empty Docker container list, awaited successful Colima shutdown, then confirmed `physharness-pilot` stopped and paused the monitor. The VM retains its pinned images for future authorized tests.

See [final evaluation](retry-final-evaluation.json), [independent live audit](live-retry-audit.md), [resource observations](retry-resource-observation.json), and [all-attempt reconciliation](final-reconciliation.json). These findings support a staged next qualification at 8–16 workers, not a claim that full-scale deployment is ready.

## Recoverable error audit

All eight typed refusals were inspected against the actual calls and succeeding actions. One checkpoint requested a 1,500 estimated-token envelope for a mandatory 1,537-token scientific core (6,147 bytes); it was refused without data truncation. The next call raised the envelope to 2,500 and saved a 9,558-byte/2,390-estimated-token checkpoint. Two attempts to attach private cross-branch checkpoint artifacts were refused; shareable Lean artifacts or ordinary messages were sent afterward. One verification call incorrectly supplied a checkpoint artifact, then used a properly submitted Lean candidate, which was accepted. A malformed artifact UUID and stale already-acknowledged discussion cursor were corrected on the next calls. Two cross-branch direct receipt lookups returned `NOT_FOUND` under the existing scope policy; the accepted dependency bundle and proof artifact remained accessible through permitted tools.

These observations show useful typed error recovery rather than absence of mistakes. No scientific data loss or proof blocker was identified. Clarifying receipt visibility in tool descriptions remains a usability improvement; bypassing branch privacy would be the wrong correction. No additional source change was needed to finish this retry.
