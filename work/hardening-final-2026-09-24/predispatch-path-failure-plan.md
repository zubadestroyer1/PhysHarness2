# Pure pre-dispatch workspace request rejection

This plan and `tests/test_worker_predispatch_rejection.py` were prepared while the cooperative attempt was live. No frozen source or canonical attempt state was changed.

Observed first fault: task `a4666609-638f-4e9b-aeb5-bb643f9b44ca` supplied `run_command` with `cwd=/work`. The pinned local provider's pure `validate_command` called `checked_path` and raised `UNSAFE_PATH` inside broker `_start`, after authority and exact VM identity checks but before operation insertion or Docker dispatch. The runtime had already saved `pending_operation` for the settled provider response. `ToolDispatcher` treated this `ExecutionError` as terminal; the native session became uncertain, the task blocked, and cleanup destroyed the VM. The failure artifact records `UNSAFE_PATH` and no operation ID. All model reservations for that task are settled; its latest response contains the matching function call and exact invalid cwd, while no workspace operation exists for that call. Preserve all of this as historical evidence. Do not replay the uncertain native session.

Observed second fault: helper `46e5908f-1c4b-4faf-931c-ee31c9494352` supplied `read_workspace_file` with `path=/work/lyap_comparison.lean`. Broker recorded pending `read_range` operation `96fd7180-b76d-5958-900a-4efdf1c15b33` before the provider's pure `checked_path` refused it. Canonical operation status is reconciliation required with `result=null`; there is no recorded original `UNSAFE_PATH` code. Native session later completed, but task blocked during cleanup because the workspace was uncertain. Root physically archived/destroyed that exact container and observed its absence; canonical retirement needs its separate narrow, evidence-bound operator procedure. Do not turn terminal model text into verified proof.

Surgical source change after the live run:

1. In `WorkspaceBroker._start`, retain current authority, fence, VM identity, provider readiness, and prior-operation checks. Catch `ExecutionError` **only** around the pure `predispatch_validate(provider)` callback, and translate a fixed set of request-validation codes to `HarnessError` with the same code and a bounded correction message. The callback runs before the pending operation marker. Provider execution and every exception after the marker keep existing uncertainty behavior, even if they use the same code string.
2. Keep `run`'s provider `validate_command` callback. Add pure pre-dispatch path and bounds validation for `upload_file`, `download_file`, and `read_workspace_range`. Move upload archive construction into this callback so path/quota validation occurs after authority but before operation insertion. Use common archive path rules plus exact provider-specific constraints: E2B `_path` caps path bytes at 256, local `checked_path` at 1024 and rejects excluded names. The provider's own validation remains for direct calls. Read offset must be nonnegative and length within 1..65536. No attempt to sanitize or silently map absolute `/work` to a relative path.
3. The research tool wrapper already catches `HarnessError` and returns its stable error envelope. Responses runtime then persists that result, clears `pending_operation`, and gives the model another turn. Change only descriptions of `run_command`, `write_workspace_file`, and `read_workspace_file` to say `cwd='.'` for `/work` and file paths are workspace-relative, without a `/work/` prefix. Do not alter scientific prompts or target source.

The nine local no-daemon regressions currently yield **five expected failures and four passing controls** before source change. The failures cover full executor behavior, a three-response native loop proving the rejected error reaches the model and `pending_operation` clears before a valid next command, and upload/download/read-range path rejection before any operation record or provider call. Passing controls prove an identical code raised after dispatch still quarantines, wrong VM identity or paused experiment wins before validation, and the actual LocalDocker provider rejects absolute paths without touching Docker. After the source change, require all nine to pass, then run adjacent broker/worker/runtime suites and the full suite. Refresh source-bound qualification and source freeze before any further paid launch.
