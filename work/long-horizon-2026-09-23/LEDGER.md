# Long-horizon implementation ledger

2026-09-23: User authorized full implementation of seven architecture layers, GPT-6 Sol coding under root planning/audit, full available VM/harness validation and two fresh $25 physics attempts with monitoring. Design/spec and task plan saved under docs/superpowers. Three independent read-only audits underway/completed; no paid calls or VM startup in this implementation turn yet.

Baseline: worktree codex/benchmark-verifier-qualification, HEAD b8a6a0b490eee440c36181d687d1be514f472615. Existing dirty documentation and untracked pilot evidence retained. Previous model-authored projection accepted only through operator follow-up submission; no autonomous completion or reuse is presumed.

Implementation must address active-context accumulation, unbounded canonical working view, blocked prior-session recovery, waiting-parent capacity deadlock, candidate submission requiring another model call, and unclear final task/experiment status. Acceptance isolation is preserved.

Root Task2 review: requested accepted-claim visibility with verified-label validation; explicit history retrieval parameters; bounded artifact reads; graph byte/edge limits; fixed manifest overhead before optional filling; adversarial failure/native/stale/pagination coverage. Three initial tests and 23 aggregate passes were insufficient to cover these new paths. Implementer fixing before acceptance.

Root Task1 integration review: active-input pruning must also bound archived raw-response growth; generic SQLite store previously overwrote history. Requested immutable archive support and exact scientific re-anchoring after compaction, with target/review drift checked before new inference.

Independent baseline checks: supported Node24.19 console tests/build exit0, metadata/source-lock validators exit0, real Temporal baseline exit0. Final counts and fresh integrated checks will be recorded after code settles.

02:03 PDT: Dedicated physharness-pilot VM started (6 CPU / 12 GiB configured); existing verifier image identity remains sha256:84deccc518a7aa5ce916d15236dac5ae416a5288449bd8620a2c8bb374c24b67. Disposable pinned PostgreSQL16 CI container created, loopback tunnel 55433 -> VM 55432. Real PostgreSQL tests passed: 3 passed, 25 deselected. No paid calls.

Root continuation audit requested: unsettled reserved usage rejection; exact source checkpoint/target/review/environment bindings; remove universal hardcoded 32-epoch ceiling; recover crashes before ticket issuance and after ticket consumption; preserve scientific workspace and approach notes; test actual one-slot waiting and Temporal resumption. Runtime independent reviewer found discarded input-prefix history absent from archives; author fixed and added tests. Native predispatch accounting-hook failure must be marked provably unsent, not an uncertain remote request.

Memory implementation second review: bounded accepted-claim index, scoped exact metadata and base64 artifact chunks, pre-reserved manifests, graph edge/byte bounds and failure/private/stale tests now present. Focused 28 memory tests passed. New disposable pilot helper delegated; exact prior review records will be imported without prior solutions to isolate the first new attempt.

Additional integrated findings (being fixed, not accepted yet): (1) one-slot runner must skip waiting parent before consuming its scheduling slot; (2) ordinary active sibling requests must not block a settled source handoff; (3) native recovery needs explicit settled-boundary marker, because usage may be settled before returned tools run; (4) handoff reason bounds differ across layers; (5) recovery idempotency must bind recovery mode; (6) workspace export and lazy restore must precede destruction/reprovisioning; (7) successor needs discoverable unverified approach/checkpoint notes, not only a fresh target. Native author takes workspace adapter lane; controller remains canonical integration owner.

Operational choice: two live known-result attempts will use an explicitly recorded low inline-compaction threshold (8192 tokens, active context cap128000) to exercise the provider feature during short runs. This is engineering qualification, not a natural long-horizon performance comparison. Whether delegation/fresh handoff occurs live will be measured, not presumed. All live calls still wait for audit blockers to clear.

Fresh reviewed-state import completed: private isolated DB has exactly 2 campaign, 2 problem, 2 review records and no prior proof/experiment artifacts. Import manifest e75ccbb2cf5a4d17aaaea8af41e249821776b1e0971f020310c37d0c3ca35789. No new scientific approval was invented.

Verifier qualification attempt-long-horizon-01 stopped honestly at core classical-positive with container_failed/Docker125: root omitted mounted TMPDIR, so /var/folders scratch was unavailable inside the VM. No Lean proof execution occurred in that failed case. Original packet retained; attempt-long-horizon-02 launched with explicit worktree-mounted scratch directory. This is a launch configuration error, not a model/proof failure. User informed.

Qualification attempt-long-horizon-02 passed all 10 mechanical checks (scope 2b5e9df5efc9f34426737791be44ec816e32850c1ab4d63a4acfbe429f3dbd77). This packet is now historical because final independent audit required further service.py changes (atomic model reservation/task binding and native artifact privacy). Root will requalify after final scope freeze. Terraform1.16.2 fmt/init/validate passed with committed provider lock; no cloud apply. New real Temporal test run first found a non-dict continuation payload guard bug (1 fail2pass); fixed rerun passed3. Additional activity-failure/retry test remains pending.

Fresh independent reviewer identified and authors are fixing: abandoned source slot after ready-ticket issuance, completed native output not finalized after process death, native opaque state exposed through generic own-branch routes, and actual activity-death Temporal qualification. Atomic source-slot settlement now occurs with continuation issuance. Private-native denial must preserve explicit portable checkpoint access. Complete checkpoint recovery must never add a model call. VM graceful handoff and fault windows retain explicit reconciliation errors when provider state is unknown.

Final integrated preflight: 892 ordinary Python tests passed (1 conditional skip, 6 integration/Lean deselections); 5 real local Temporal tests passed, including canonical continuation after a lost activity reply; 3 real PostgreSQL tests passed; 36 console tests and production build passed. Ruff checked/formatted 131 files; metadata and formal-lock validation passed. Terraform 1.16.2 readonly-lock init/validate passed; application image built and isolated import smoke passed. Independent reviewer found no unresolved high-severity issue in reviewed paths. Native workspace restoration remains fake-provider contract evidence, not live E2B qualification.

Final verifier qualification attempt-long-horizon-03 passed all 10 mechanical checks against source scope 8b06e5b17727e25d8b003e5c943b92ed2f61ecdb94e2b950db74d7cc2d5971e1. Immutable packet production/deployment approval remains pending; existing user authorization covers this local pilot only. Registry pinned to this packet and the unchanged reviewed target bundles. No expert or production qualification is invented.

Live projection attempt-01 fcacc4fd-148c-4d8f-b614-0ca5ca9b76e8 finished autonomously with 2 completed tasks, 1 delegation, 42 native compactions, 3 rejected candidates and 2 independent-kernel accepted submissions. Cumulative usage 1,674,571 tokens; ledger model cost $3.419262, no active workers/reservations/uncertain operations. No operator submitted a proof or injected a previous solution. No fresh-session handoff occurred. Runner completion and proof acceptance are separate; canonical experiment lifecycle remains queued until explicitly paused/cancelled.

Live purity attempt-01 041d11ee-e488-45f5-863f-41cc58c5a67d launched with identical recorded model/configuration and a separate $25 shared envelope. First accepted artifacts are available through permitted research retrieval; actual use must be measured. Dedicated VM observed 514 MiB used of 11,933 MiB, 11,419 MiB available, no observed OOM. Evidence and raw model archives remain separate; public reports contain safe accounting and provenance only.

Purity attempt-01 ended with no verified proof: 9 blocked compilation submissions, 77 native compactions, 2 delegated tasks, 3,876,074 cumulative tokens, $7.878868. The shared 4M token envelope refused the next 144,384-token conservative reservation; zero active/reserved/uncertain work remained. Terminal audit found the refusal mislabeled PROVIDER_FAILED via a generic runtime hook wrapper. GPT-6 Sol author added narrow typed token/cost admission errors and regression tests; root full host suite passed894, independent re-review and operation correlation finishing. No historical outcome is rewritten. This was a local diagnostic fault, not provider/VM crash; no paid rerun is needed to reproduce the deterministic admission error.

Artifact integrity: all 675 projection and 1,167 purity records hash-verified; private exports and consistent SQLite backup preserved. Both experiments paused after completion. Disposable PostgreSQL container removed; dedicated VM observed stopped, Docker unavailable, SSH DB tunnel closed, monitor PAUSED. Host-only final regression work continues without the VM. Accepted proofs remain exactly bound to prior source qualification; all61 scoped acceptance inputs remain unchanged after the diagnostic patch.

Final source settled after operation-ID diagnostic addition: 894 host tests passed, 1 skipped, 6 deselected; exact CI Ruff paths passed131 files; git diff --check passed. Independent audit rechecked token/cost admission and unsent-request handling. Initial final-lint command erroneously included nonexistent scripts directory; historical error retained and exact CI command rerun successfully. Both live tests and cleanup are complete; report distinguishes1/2 accepted targets from implementation/testing completion.
