# Controller continuation implementation audit — 2026-09-23

The controller now issues a new native Responses session only from a settled, terminal `handed_off` source checkpoint with exact task, experiment, target/review/environment, model, runtime-limit, checkpoint-digest and source-fence bindings. The agent may request a handoff or wait for delegated child tasks; the controller alone issues and consumes the ready ticket. One-slot parent waits free capacity for a child and resume only after each waited child reaches a terminal scheduling status. A child failure is reported by status/error code, never silently treated as proof. The distinct-task budget counts each task once across continuation epochs.

The native runtime settles provider usage and tool outputs before a handoff hook, marks unsettled intermediate checkpoints, and rejects uncertain replay. Task model reservations are now inserted atomically with the budget reservation under the current task fence. A crashed controller can reconcile a settled task binding; an expired orphan worker slot is released only if every linked VM is confirmed destroyed and no model reservation remains active. Ticket issue settles the source shared slot in the same transaction, so a crash after ticket issue cannot strand one-slot capacity. A consumed ticket can return to ready only when no successor native session exists. A safe persisted first or successor `running`/`interrupted` session can be adopted under a fresh lease; uncertain/pending model or tool operations remain blocked. A persisted completed session can finalize its exact saved output under a fresh fence without another model call, including an idempotent post-output/pre-finish retry.

VM workspace export is bound to a fenced task before source cleanup, then the exact archive and policy hashes are carried in the ticket. A source-fence VM record without a durable archive prevents recovery into an empty successor VM. The successor restores to a distinct execution identity before scientific tools or model generation. This is exercised by fake VM byte-roundtrip tests; no live VM handoff claim follows from those tests.

The worker starts and reanchors after compaction from bounded canonical working context, exact target/review/approach notes and unresolved obligations, with notes labelled unverified. Native checkpoints, archives, runtime events, execution failures, model-reservation records and native sessions are hidden from agent generic reads, while an agent may read its own branch's portable scientific checkpoints. Bounded history, index, graph, record, artifact chunks, mailbox, notes and candidate submission have worker and authenticated API/client/MCP surfaces. Candidate submission stores exact source and queues independent verification; it does not assert proof acceptance.

Local focused validation through the last controller edit: 11 controller continuation tests, 4 memory API tests, 4 handoff-notes tests, and 51 earlier combined orchestration/research/memory/workspace tests passed. Fault coverage includes one-slot parent-child-parent flow; terminal post-issue/pre-finally crash; consumed-before-first-save crash; first-session settled running/interrupted adoption; unsettled rejection; atomic model binding; completed-native/pre-output and post-output/pre-finish recovery; source slot leak; and workspace archive identity/byte roundtrip. The real Temporal continuation tests are opt-in because they launch a local Temporal CLI. Root reports four passed before the newest fifth test; the fifth exercises actual canonical executor handoff with a lost activity result and is pending its escalated run. Temporal uses three bounded activity attempts; canonical runtime/ledger checks decide whether a retry can safely proceed.

A full local default suite run after privacy tightening gave 873 passed, 7 skipped and 19 failures, all in preexisting `tests/test_sharing.py` expectations that agents can see opaque own/trusted native state. The memory owner is updating those tests to match the explicit new privacy contract; the full suite must be rerun afterward. The fresh source qualification collector and paid two-target experiments are owned by the root task. This report is implementation and local test evidence, not a live-fleet, cloud-recovery, autonomous-discovery, or formal proof claim. Projection's existing operator-submitted verified model artifact remains separately attributed and must not be described as an uncontaminated autonomous discovery.

## Shared envelope diagnostic correction

A later bounded mock reproduction showed that a canonical token-envelope refusal during `generation_started` was wrapped by the native runtime as `PROVIDER_FAILED` before the worker reported it. The worker now translates only the two explicit, pre-provider ledger admission failures `TOKEN_BUDGET_EXCEEDED` and `BUDGET_EXCEEDED` into typed execution errors with their original code and remediation. Other persistence/provider failures remain generic or uncertain under the existing rules. The failure artifact records the exact envelope message, and the team report keeps the precise code on that task outcome; a mixed team run does not get an invented global stop reason. Mock token and cost exhaustion tests verify no provider generation, zero active/reserved capacity, no uncertain reservation, and matching task/report/artifact codes. Focused research/orchestration/controller suite: 43 passed; Ruff passed. This correction did not change accounting, trust, acceptance, or verifier-scoped `service.py`.

The admission error retains the generation operation ID for correlation; the controller-private failure artifact records that ID without suggesting a provider call occurred. Both bounded exhaustion cases re-passed after this addition. The full default host suite was run during the correction and reported 894 passed, 7 skipped; the final operation-ID addition was then covered by the focused two-case test and Ruff. No verifier-scoped service change was made for this diagnostic correction.
