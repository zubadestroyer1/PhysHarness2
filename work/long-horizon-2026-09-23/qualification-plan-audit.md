# Long-horizon pilot: qualification and execution audit plan

This is a read-only planning record. No VM, verifier, model run, or paid call was started for this audit. The proposed next condition is two **new** experiments against the already reviewed projection and purity targets, with a separate $25 central cost ceiling for each. Preserve all earlier experiments and receipts. The exact long-horizon token/context policy must be reviewed after the code change; the current `operator_helpers.py prepare` still requires 96,000 aggregate tokens and 24 turns, so using it unchanged would reproduce the exhausted allowance.

## Current baseline and its limits

- The pinned image is `sha256:84deccc518a7aa5ce916d15236dac5ae416a5288449bd8620a2c8bb374c24b67`. The dedicated Colima profile is `physharness-pilot`, with `DOCKER_HOST=unix://$HOME/.colima/physharness-pilot/docker.sock`. The VM was recorded stopped with its endpoint unavailable after the live follow-up. Recheck the actual endpoint and host agent on recovery; `colima status` alone is insufficient.
- `work/pilot-qualification-2026-09-23/attempt-01` froze scope digest `f13defb821a8043a16978a83058abd1e6f7cbed8a60bd36549fb647efe3cd70f`: 61 input hashes, exact image and runtime identity, 24/24 core/library fixture outcomes in kernel and independent modes, 16/16 fixed boundary observations, and all 56 required regression identities (226 passes, one conditional PostgreSQL skip). The two selected reference proofs and one negative control each produced 3/3 expected outcomes in both modes. This is engineering evidence; `production_qualified=false`, deployment approval remains pending, and the known proof fixtures are not model discoveries.
- Both targets have reviewed identities: projection problem `b3817784-8d0e-4961-9903-c3c0feef1b58`, purity problem `215b5024-4285-4aa4-baed-05cf37363291`. Preserve their target/review/challenge/environment digests. Their source statements and caveats are in `work/first-pilot-review.md`.
- Projection experiment `ff884e1f-6600-40c2-a704-4072c2c163f4` exhausted 93,963 of 96,000 aggregate tokens with task `BUDGET_EXHAUSTED` and three blocked receipts. Later, the operator submitted an unchanged fourth model artifact, SHA-256 `0fc36413e53a2ea0f599819310b15d6cd57d290710b3395f9b75ceaedbb2c762`; receipt `d3c9db0a-a9f5-42ea-9c10-61661a0f31b3` is `verified`, `kernel_checked`, `independent_kernel`. Its claim is real, but it was an **operator-assisted final submission after the autonomous run**, not an autonomous completed loop. Purity experiment `9d35efef-dbcd-49c4-9665-b36b492fa7cc` was not launched. Do not describe a fresh projection rerun as uncontaminated discovery: the target, known reference proof fixture, and now a verified model artifact exist in this workspace.

## Checks after implementation, before paid execution

1. Review the code diff against the old live failure: active Responses input must be bounded by automatic compaction/continuation, while durable scientific checkpoints and verified source retrieval remain usable. Check what counts toward central reservations, how exhausted resource and terminal task state appear in run summaries, and that a failed or interrupted call cannot create unaccounted workers, reservations, or proof claims. Run focused tests that exercise the actual long-context transition and accounting/restart behavior, plus `uv run ruff check src tests tools infra migrations`, `uv run ruff format --check src tests tools infra migrations`, and `uv run pytest -m "not integration and not lean" -q`. If changed paths affect CI's Temporal, PostgreSQL, console, Terraform, or container responsibilities, run those relevant CI jobs too (`.github/workflows/ci.yaml`). CI's ordinary Python job also runs `uv run python infra/validate_metadata.py` and `uv run python tools/formal_environment.py validate`; it does **not** qualify the Linux proof boundary.
2. Compare changed paths and hashes with the frozen `scope.json`. Changes to its 61 scoped files, image driver/resource policy, verifier launcher, registry, acceptance or qualification code, resource profile, or Docker runtime mean the old packet is not current qualification evidence for the changed condition. Changes only to model runtime, memory, team controller, or operator helper may leave the verifier scope bytes intact, but the prior packet does not validate their new long-horizon behavior. Record that distinction explicitly and run focused integration/failure tests; do not relabel a prior verifier packet as new controller evidence.
3. If the verifier scope/runtime has changed or a fresh current-source qualification is required, start only the named dedicated VM, verify the exact Docker endpoint and image ID, then run the durable collector with a **new** attempt name. From this worktree after Docker is ready:

   ```sh
   unset COLIMA_HOME
   export DOCKER_HOST="unix://$HOME/.colima/physharness-pilot/docker.sock"
   PYTHONPATH=src .venv/bin/python .state/pilot-qualification/collect.py all --attempt attempt-long-horizon-01 --image-tag physharness-formal:wave01-resources
   ```

   The byte-identical public copy is `work/pilot-qualification-2026-09-23/collect.py`. Its serial stages are `capture`, `regressions`, `core-kernel`, `core-independent`, `library-kernel`, `library-independent`, `boundary`, `assess`. Use one new attempt directory and never overwrite or splice results. The collector insists on image ID `84de...b67`, compares the embedded driver, resource policy, and source lock with current files, freezes current scope, then checks it after each stage. If embedded code changed, rebuild and establish a new image identity and collector expectation; the old digest cannot be claimed for new image bytes. The 24 checker executions each have a 600-second upper bound, so budget hours, not minutes. Audit the new packet's actual checks, regression JUnit/exit code, reports, container cleanup, and remaining human review gates before relying on it. Preserve the separate selected-target fixture checks if target/verifier inputs changed; do not count their known proofs as live model results.
4. Verify the live pilot's target reviews and scoped deployment decision are still bound to the **current** target, environment, image and registry. Any changed bundle, registry, image, review, or target revision requires new review rather than borrowing the old decision. Check the `physharness-pilot` runtime has capacity for the 8 GiB/4 CPU/600-second/one-checker profile on its 12 GiB VM, and no orphan checker containers. The prior packet explicitly leaves process-exit, complete host containment, resource stress, PostgreSQL review race, and fleet capacity gaps; do not claim production qualification.

## Fresh experiment sequence and observation

1. Define and record the new controller configuration, including larger aggregate token allowance and context/turn limits, the same `gpt-6-sol` Responses model/high reasoning and verified sharing unless intentionally changed, one worker, 1,800-second bounded run, and $25 central ceiling **per experiment** ($50 maximum for the pair). Update the canonical preparation helper or use a reviewed equivalent so it validates the new contract rather than silently copying the old 96,000-token settings. Check current pricing and the applicable per-request context tier before launch. Choose unique attempt names; helper idempotency returns the same experiment if name and target repeat.
2. With the operator's private environment loaded, create each new experiment against the two existing reviewed problems through the canonical service. `work/live-pilot-2026-09-23/operator_helpers.py prepare projection|purity --attempt NAME` is the established interface, subject to the contract update above. Record returned new experiment IDs and exact plans. Keep private auth/key files and native checkpoints out of logs and public evidence. Run `uv run phys check-run ID` separately on each fresh experiment and resolve all blockers before launch. The prior `projection` experiment stays paused; the old `purity` experiment stays unlaunched.
3. Run projection first, bounded by `uv run phys run-team ID --concurrency 1 --max-tasks 1 --timeout-seconds 1800`, capturing its raw JSON stdout only under the private pilot directory. Use the helper's `report` extractor for allowlisted public accounting and `snapshot` against the explicit pilot DB for timestamped status, tokens, reservations, workers, receipt references and checker versions. Poll at a reasonable interval during execution and at terminal state; compare session usage with the central ledger and the actual task outcome. The team status alone is insufficient: the first attempt ended with an exhausted task while the experiment still appeared queued. Preserve any incomplete/blocked state, artifact hashes and diagnostics in private evidence. Audit actual compaction/continuation events and whether the autonomous loop submitted its own successful candidate before describing it as long-horizon success.
4. Launch purity only after inspecting the projection's exact `independent_kernel` verified receipt and retrievable accepted source. Run its new preflight and the same finite supervisor. Audit retrieval calls, second candidate source, cited first claim/receipt, and final independent receipt to determine whether the accepted lemma was actually incorporated and rechecked. A direct proof of purity is mathematically useful but does not demonstrate lemma reuse. Distinguish autonomous verification from any later operator submission.
5. Finish with canonical records, receipt/artifact/target/environment/review bindings, hashes, ledger spent/reserved amounts, uncertain operations, active worker count, checker cleanup, and a final allowlisted report. Pause/close any live experiment that could allocate further work; stop the named VM and verify endpoint unavailability plus host-agent absence. Record the shutdown and retain all failed and successful attempts. Do not publish native checkpoint contents, credentials, private proof sources, or raw model context.
