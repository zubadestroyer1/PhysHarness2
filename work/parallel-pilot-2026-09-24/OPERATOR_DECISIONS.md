# Root review decisions for the authorized $100 pilot

- The user's 2026-09-24 request authorizes this new $100 ceiling and delegates target review to the root assistant. The two exact reviews are explicitly assistant-led, not independent human expert review. No publication or novelty approval is given.
- Replace the previously proposed Bell conjunction with a dimension-general dephasing identity and nonnegativity; strengthen oscillator energy monotonicity with both trajectory component bounds. Both have complete withheld references and public worker targets. They are known results, with unmeasured model difficulty. Do not mistake short successful proofs for a collaboration or long-context qualification.
- Four sequential arms compare independent and communicating pairs; two active workers per arm, fixed model/runtime/library/budget. $24.95 each plus at most $0.20 for the single API probe totals $100. Unspent funds are not permission to create an extra full-budget attempt. All retry/repair work preserves incurred and uncertain costs.
- Use gpt-6-sol high reasoning, 256000 active context, 64000 per-response output allowance, research compaction at184000, no cumulative token cap,1000 native-session turns with existing handoff mechanism. Use a finite24-hour session/experiment/supervisor limit for this pilot (increased from2hours). This is not month-scale execution qualification; monitor for any deadline approach rather than claim dollar-only termination.
- Official model rates observed2026-09-24 are ordinary input$2/M, cache writes$2.50/M, cached reads$0.20/M, output$10/M. Price all input at$2.50 for conservative accounting; ledger is an upper estimate, not exact invoiced cost. Active input stays below272K surcharge threshold. Standard processing, no paid hosted tools/fast-tier override.
- With full-window reservations, up to$1.28 per arm may remain unavailable for a new full-capacity request near exhaustion. This is explicit reservation headroom, not a lifetime token limit. Reducing request size dynamically is deferred; never send unreserved calls. Actual output-cap hits must be counted from native incomplete_details, not guessed from a generic failure code.
- The local provider qualifies one controller host and one canonical dedicated Unix Docker socket. Stable host flock does not coordinate distinct hosts/proxies to the same daemon. That broader deployment needs a central authority. Unknown orphan workspaces remain visibly blocked for operator reconciliation; do not claim automatic recovery from every provider ambiguity.
- At launch, unrelated containers block readiness. During per-task dispatch the same daemon-occupancy check is launch-only because the legitimate independent verifier may be active. Capacity and memory checks still block dispatch; Docker admission is rechecked atomically. Preflight memory is a capacity estimate; actual VM memory pressure is observed before launch and during the real test.
- A provider overrun is already incurred: preserve actual usage, mark reconciliation required and stop new allocation. This detects an exceptional billing violation; it cannot retroactively guarantee the provider obeyed its requested bound.
- Do not commit or publish private native transcripts or reference proofs. Preserve preexisting dirty work. Keep failed test logs and qualification attempts. Stop dedicated VM after required tests/evaluation and pause the monitor.
