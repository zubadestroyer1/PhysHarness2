Implement configurable local Docker workbench capacity. Work in this worktree. Own src/physharness/execution/local_docker.py, tests/test_local_docker_workbench.py and new narrowly related capacity tests ONLY. Default max_active_workspaces=1; configurable positive bounded integer <=100. Preserve fixed per-worker 2GiB/2CPU and isolation. Cross-process atomic admission must use Docker-daemon-visible guard or a truly stable common lock (not TMPDIR-dependent); concurrent capacities must not silently disagree or oversubscribe. Report design tradeoffs. Count active labeled workbenches accurately; third refused with capacity2; failed create/cancel cleans own resources, never another owner. Tests cover parallel create and cleanup and malformed capacity. Add read-only provider capacity/resource probe useful to preflight, coordinate interface with root. No cloud/model calls, no VM starts, no commits. Do not spawn subagents. Run targeted checks; report work/parallel-pilot-2026-09-24/capacity-report.md with changes, exact commands/results/risks. Preserve unrelated dirty changes. Read existing provider, errors, tests first. Root audits code.
