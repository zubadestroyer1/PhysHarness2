# Four-arm operator execution helper

Work in current worktree. Own work/parallel-pilot-2026-09-24/pilot_ops.py and tests/test_parallel_pilot_ops.py. No further source edits unless root approves. Need minimal launch/status helper, no new framework. Root prepares problems, assistant reviews, bundles and registry. No live model calls or VM lifecycle yourself.

Read private config from explicit --config argument (root creates `.state/parallel-pilot-2026-09-24/config.json`). Proposed schema:
```
{"project_id":"parallel-pilot-2026-09-24", "private_directory":".../private", "database_url":"sqlite:///.../private/harness.db", "artifact_root":".../private/artifacts", "registry":".../verifier-registry.json", "model_prices_file":".../prices.json", "worker_qualification_file":".../worker-qualification.json", "docker_host":"unix://~/.colima/physharness-pilot/docker.sock", "worker_image_digest":"sha256:48e4f60a07c289baf846c0ff6fa2624870c59547c2a00971db0510e767161be0", "arms":[{"label":"quantum-independent","experiment_id":"..."},...], "aggregate_ceiling_usd":"100.00", "probe_envelope_usd":"0.20", "arm_ceiling_usd":"24.95", "status_file":".../status.json", "results_directory":".../private/results"}
```
Use issued operator identity from auth.json via token file, don't invent identity. Secrets stay out of config/log/status; OPENAI_API_KEY loaded externally by root process. Fresh Settings explicit to avoid inherited unrelatedpilot env. Auto_create_schema false. Bind WorkspacePolicy exact problem environment_digest and SHA256 workerqualificationfile, timeout86400, localzero cost, capacity2. Run preflight requested_concurrency2 before seeding/paid work.

Validate exactly four unique experiments, <=24.95each and sum envelopes+probe.20 <=100; exact runtime Responses gpt-6-sol high, max_total_tokens null, max_context256000/max_output64000, maxturns1000, budgetmaxconcurrency2, maxruntime86400; all exactsameproblem pair byprogram with independent vs ideas sharing, no unknown/newarm launch. Start CREATED only, do not auto resume/retry uncertain work. Run arms sequentially in config order through canonical Activities.apply_experiment_command + ResearchTaskExecutor + ResearchTeamRunner. After transitionstart before seeding configure workforce max_total_tasks128,max_pending128,synthesis0. TeamRunManifest concurrency2,max_tasks128,max_verifications128, timeout86400. Root may later separatelyaudit/resume failedarm withinoriginalledger; helper exits loud on crash/uncertainty and preserves results.

Persist per-arm result JSON privately; allowlisted public status every10seconds: UTC, currentarm, experiment/task/session statuses and counts, ledger, verifiedreceipt ids/status/assurance, compaction count if easy, messages/delegations counts. No prompts, proofsource, messagebodies, nativecheckpoint/credentials. Atomicreplace status. Errors include stage and safe error code; raw exception only private log withnoenv dump. Exitnonzero on blocked/error, don't silentlycontinue failto next arm. Full private result exports can be done root later.

Make start gate non-reentrant (stable flock inprivatepath), refuse if arm already noncreated or previousresults unless status-only. Document status read needsnoAPIcalls. Tests with injectable runtime/service minimal fakes for aggregate refusal, duplicatearms, privacy, stoponfailedarm, idempotent no accidentalrelaunch. Can reuse canonical realrecords fixtures and mockedResearchTeamRunner. Keep short; do not rebuild preparation, reviews, snapshots, service logic.

Report helper invocation and schema to work/parallel-pilot-2026-09-24/launcher-report.md; root audits before launch. No subagents, commits, paidcalls orVMstarts.
