Implement launch capacity and memory readiness checks before paid work. Own config.py, orchestration/workspace_selection.py, orchestration/workspace_tools.py, run_control.py, cli.py, and focused tests (do NOT edit local_docker.py). Provider contract agreed: LocalDockerWorkspaceProvider(..., max_active_workspaces=1), default one; configurable <=100. Wire Settings worker_max_active_workspaces and factory argument through restoration/probe. Coordinate with capacity implementer. Actual requested run-team concurrency (not merely budget ceiling) must reach preflight. For local Docker verify worker concurrency <=configured capacity; available daemon memory covers requested 2GiB workers + 8GiB verifier + explicit reasonable headroom (propose2GiB), CPU oversubscription report vs hard fail only justified. Docker info errors fail loudly. Preserve E2B behavior. Expose machine-readable checks and meaningful coded errors; no model calls on rejection. Tests direct CLI/preflight/factory, legacy one default. Avoid universal new constraints on unrelated run modes. No cloud/model calls, VM starts, commits or subagents. Report work/parallel-pilot-2026-09-24/preflight-report.md with exact tests/results/limitations. Shared dirty work must survive.
