# Current-source verifier control capture

The 2026-09-23 `attempt-01` engineering capture used the restored consolidated image `sha256:84deccc518a7aa5ce916d15236dac5ae416a5288449bd8620a2c8bb374c24b67` on the dedicated `physharness-pilot` Linux VM. The operator verified the retained archive hash and recovered that exact image in a fresh VM. The [frozen scope](attempt-01/scope.json) has canonical digest `f13defb821a8043a16978a83058abd1e6f7cbed8a60bd36549fb647efe3cd70f`. The [qualification packet](attempt-01/QUALIFICATION_REVIEW.md) records all ten automatic checks as observed: 24/24 core and library fixture outcomes across kernel and independent modes, 16/16 fixed boundary checks, and all 56 required host regression identities passed. The [JUnit record](attempt-01/regressions.xml) contains 227 tests: 226 passed and one conditional PostgreSQL skip. The separate [selected-target kernel](attempt-01/selected-targets-kernel.json) and [independent](attempt-01/selected-targets-independent.json) reports each match 3/3 expected outcomes: two known reference proofs accepted and one altered control rejected. These are engineering fixture outcomes, not agent-discovered proofs or scientific review. The [independent audit](independent-audit.md) reassessed the packet and selected-target evidence.

The [retention and shutdown record](retention-and-shutdown.json) shows that the named persistent Colima profile retained the exact image and ten runtime identity fields through a stop and restart, had no checker containers, and was finally stopped with its Docker endpoint unavailable. The packet remains `production_qualified=false`, `deployment_approval="pending"`, and `scientific_review="not_provided"`. An authorized reviewer must establish evidence provenance and decide deployment and target semantics separately.

This directory is reserved for fresh engineering evidence for the current source tree and the restored consolidated image `sha256:84deccc518a7aa5ce916d15236dac5ae416a5288449bd8620a2c8bb374c24b67`. Historical `work/wave01/evidence-8g` records describe a different source scope and are not inputs to this capture.

The operator runs `.state/pilot-qualification/collect.py` from the repository worktree with `PYTHONPATH=src`; a byte-identical durable copy is retained as `collect.py` in this directory. The script requires the restored image under the default tag `physharness-formal:wave01-resources` or an explicit `--image-tag`. It reads the actual local image ID and the image's embedded build metadata, source lock, driver and resource policy, then records the current Docker runtime identity. It uses `capture_scope` to freeze the input snapshot before any checker run. A different image ID, changed embedded source, or changed scoped input stops the attempt.

For this pilot, the operator created the explicit Colima profile `physharness-pilot` under `~/.colima`, with its Docker socket at `~/.colima/physharness-pilot/docker.sock`. From `/Users/kieranpi/Desktop/Projects/PhysHarnessV2/.worktrees/formal-research-loop`, after that VM and Docker daemon are ready:

```console
unset COLIMA_HOME
export DOCKER_HOST="unix://$HOME/.colima/physharness-pilot/docker.sock"
PYTHONPATH=src .venv/bin/python .state/pilot-qualification/collect.py all --attempt attempt-02 --image-tag physharness-formal:wave01-resources
```

The first restore attempt used `DOCKER_HOST=unix://$HOME/.local/share/physharness-colima/default/docker.sock` and failed because that socket did not exist. The operator preserved its stderr in `.state/pilot-qualification/image-restore.log`. Colima 0.10.3 had used its default home because the requested `COLIMA_HOME` directory had not existed at startup. That failure is an operational finding, not verifier qualification evidence. The operator stopped that initial default VM and started the named profile above; no qualification control was run as part of that failed restore.

The `all` stage runs the matrix-selected host regressions with JUnit and an actual exit-code sidecar, the nine core and three library fixtures in both kernel modes, the fixed sixteen-observation boundary probe, then the existing `assess_qualification` API. It runs serially. Budget several hours for the real checker controls: there are 24 case executions and each Comparator has a 600-second upper bound, so the timeout-only ceiling for those cases is four hours, plus setup, regressions, and assessment. Normal completion may be much quicker. The command exits nonzero on a failed stage and leaves the reports and logs captured so far.

For operator-controlled staging, run each stage in this order with the same `--attempt`: `capture`, `regressions`, `core-kernel`, `core-independent`, `library-kernel`, `library-independent`, `boundary`, `assess`. A stage refuses an existing output. After a failed attempt, use a new attempt name; never overwrite an observation or splice reports from different attempts. Each attempt has its own evidence directory here and scratch directory under `.state/pilot-qualification/`.

The packet records mechanical checks and the retained review gates. The satisfied automatic checks leave `production_qualified=false`, `deployment_approval="pending"`, and scientific review unprovided. The operator must still establish the provenance of the Docker and JUnit observations. This collection excludes the optional process-exit probe and the separate sixty-case physics benchmark.
