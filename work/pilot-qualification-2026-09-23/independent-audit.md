# Independent audit of the prepared pilot

The current-source engineering packet at `attempt-01` was reassessed offline from its frozen scope and actual suite, boundary, regression, and JUnit files. `capture_scope` still equals the frozen scope; `assess_qualification` reproduces the saved packet exactly: ten observed checks, mechanical status `satisfied`, `production_qualified=false`, `deployment_approval=pending`, and scientific review `not_provided`. The regression sidecar records exit code 0; its JUnit hash matches, and all 56 matrix-required test identities are present and passed among 227 test cases. Core outcomes match 9/9 in each mode (2 verified, 7 blocked), library outcomes match 3/3 in each mode (2 verified, 1 blocked), and the fixed boundary report has 16/16 true checks. The packet remains an engineering observation subject to human provenance and deployment review.

The selected-target fixture hash is `32d532f8244e91af72ed64c7a047c5c62c72703ed5c14323f620a50fa6a1a076`. Its two positive challenge files match the active v2 challenge bytes and hashes (`b9a6382b1ebe426f4c580d11c743a6a89968fcf5ea756ccc329ba975497a2667` and `9d67036429d2fa386e403b21d35ad2839115267f4fb2281f511d6d54d27417ba`). Both selected-target reports use that fixture hash and the frozen image, runtime, driver, launcher, seccomp, and resource digests. In both kernel and independent modes, the three outcomes match their expected status and code: both known reference solutions verify, and the altered missing-mixture-weight control is blocked with comparator exit 1 and Lean `unsolved goals` on the diagonal entries. Both positive independent outputs explicitly report acceptance by Nanoda and Lean's default kernel. All six containers have unique names and removal records with exit code 0. I compared challenge bytes and outcome metadata without reading the reference proof bodies.

These selected-target results show that the independent verifier accepts the two **known fixture proofs** and rejects the altered control. They are not model results, proof discovery, scientific approval, or evidence that the second model attempt reused the first result. The active v2 plans allow 24 turns and cap each proposed GPT-6 Sol/high experiment at $5, one worker, 1,800 seconds, and 96,000 total tokens. The saved key-loaded preflights still block on exact target review and verifier activation; each records `model_calls=0`. The proposed registry has two distinct active v2 problem revisions; both bundle manifest hashes and resource source hash match their files, and its qualification digests match the frozen scope and saved packet. Its existence does not change the packet's pending approval.

The formal statements use a fully specified half identity/half Pauli-Z conjugation on a qubit density matrix. `MState Qubit` supplies the physical-state assumptions used for the purity identity, including Hermiticity, positivity, and normalization. The first theorem states matrix diagonalization and idempotence, without separately proving a CPTP certificate. The second states a complex trace equality whose right side coerces the real `Complex.normSq`; nonincrease of real purity is its interpretation, not an additional formal conclusion. Both are finite-dimensional, known development examples; neither is a novelty claim. A physics/Lean reviewer must still approve each exact target and its physical interpretation before paid work.
