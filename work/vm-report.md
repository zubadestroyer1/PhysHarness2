# VM workspace durability implementation report

Owned files only: `src/physharness/execution/e2b.py`, `tests/test_execution_e2b_durable.py`, `docs/VM_WORKSPACES.md`, this report. No commits, child agents, real VM allocations, provider snapshots, paid calls, cloud apply, or credential reads.

Implemented safe bounded file upload/download and canonical file-only JSON archive export/restore; outer/file SHA-256 validation; traversal, symlink, hardlink, device, duplicate/path-shadow rejection; empty-or-identical restore; source identity checks and operation serialization. Transfers use real `commands.run` with isolated Python helper and no-follow directory descriptors. Limits: 64 KiB archive, 32 KiB/file, 64 files. Entire-directory transactional restore and consistent export under concurrent guest writes are not claimed.

Implemented native pause/full-memory resume, source resume after snapshot, snapshot creation, pinned native restore into a new identity, and actual native fork into an independent identity. SDK 2.49.1 returns SnapshotInfo(snapshot_id,names), and fork returns list[sandbox|Exception]. No invented immutable hash field. Exact source/template IDs and record hash validated; real source/child SandboxInfo policy fields used for no-egress verification. `:latest` and unversioned snapshot restores fail; version-looking tags still require explicit operator qualification because the SDK cannot prove immutability. New allocations force no internet, secure mode, no environment credentials. Invalid native child policy triggers termination.

Native mutations require injected durable CommandJournal-compatible store plus operation ID. Journal includes template/workspace/timeout and operation inputs. Pending work stays uncertain on failure; allocation replay never creates another VM. In-memory replay returns known child; after restart explicit child identity reconciliation is required. Resume supports actual class-level SDK connect when local handle absent. Native capabilities are separate from generic execution protocol and always mark live_qualified false.

Exact new public contracts are documented in docs/VM_WORKSPACES.md. No types.py, __init__.py, generic protocol, controller, schema, registry or API edits. Production parent needs canonical begin/complete journal adapter, artifact/hash storage, persisted native checkpoints/child IDs, VM-budget reservation before native mutation, reconciliation/GC, and live pinned-template qualification. Do not infer these are already wired merely because adapter methods exist.

Qualification: unit and actual local helper execution; genuine installed SDK signatures/models and primary E2B SDK source inspected. Mocked native calls only. No live cloud security, native memory correctness, snapshot pin immutability, or service-version qualification performed.

Final verification: `ruff check` clean; `ruff format --check` reports both files formatted; `pytest -q tests/test_execution*.py` **68 passed in 1.74s**. The new durability file contains **20 passing cases** (including parametrized malicious paths). Tests execute the adapter-authored helper in isolated local temporary directories and mock cloud SDK calls. Covered canonical roundtrip/checksum tamper, duplicate/link records, traversal and path shadowing, symlinked root/ancestors, hardlinks, quotas, partial/conflicting restore refusal, identical restore replay, stale identities, checkpoint JSON/tamper, pause/resume and reconnect, independent child shutdown, source/child no-egress checks, mutable snapshot rejection, restore after source deletion, changed replay configuration, lost child handle, and explicit source resume after snapshot. Installed SDK version re-read as 2.49.1 and native method/dataclass signatures confirmed.

# Canonical WorkspaceBroker follow-up

Added only `src/physharness/orchestration/workspaces.py`, `tests/test_workspace_service.py`, and this report/docs for canonical integration. Also changed E2B adapter/tests under explicit parent authorization to fix independently audited provider races and failure cleanup. No core/domain/service/research_worker/schema/API edits; parent owns worker integration.

Broker constructor: `(service, *, actor, task_id, holder, fence, provider_factory, provider_spec, worker_slot_id=None)`. Provider factory is synchronous `factory(*, journal)` returning an unallocated adapter. Public methods: `provision(*,cost_bound_usd,operation_id)`, `inspect(workspace_id)`, `run(workspace_id,*,expected_execution_id,request)`, `upload_file(workspace_id,*,expected_execution_id,path,data,operation_id)`, `export_workspace(workspace_id,*,expected_execution_id,operation_id)`, `destroy(workspace_id,*,expected_execution_id,operation_id,actual_cost_usd)`. All return dictionaries; run returns CommandResult JSON. Canonical journal `begin`/`complete` persists native operation identities/results and checkpoint artifacts; native child allocation is blocked until separately budgeted broker lifecycle exists.

Atomic canonical allocation intent/workspace/reservation before provider allocation. Explicit positive conservative cost bound, exact matching provider specification, timeout fitting remaining experiment runtime, whole-experiment cost/concurrency ledger. All inputs, task holder/fence, provider spec and shared-slot ID included in replay fingerprints. Every dispatch and completion checks current lease and cancellation/experiment state; cancelled cleanup allowed only current fence and known execution ID. No success after lease loss during a call. Uncertain outcomes preserve cost/concurrency reservation and record loud reconciliation-required state; even known destruction under stale ownership does not settle. DB failure while recording provider outcome retains known IDs in actionable error details and in-memory observations; canonical pending record prevents duplicate dispatch when DB recovers.

Shared-slot path implemented at parent's request: optional slot must match task.payload.worker_slot_id and a same-experiment ReservationRow with state='active', workers=1, under current lease. A slot cannot have more than one unresolved workspace. VM cost reservation uses workers=0; destroy never settles shared parent slot. Parent must keep that parent slot held until all referencing workspaces are confirmed destroyed. Standalone additional worker-slot path remains supported. `inspect` allows operator observation after lease loss; no mutation authority follows from inspection.

Tests cover ledger/cost/concurrency reservations before allocation, lifecycle artifact roundtrip, operation replay, changed inputs, concurrent provision, restart uncertainty, stale lease during allocation/command/destruction, cancellation denying execution while allowing fenced cleanup, unknown execution IDs, actual-cost settlement, uncertain cleanup holding resources, native journal/checkpoint artifacts, native pending-operation serialization, agent authority rejection, cross-task scope, current status on provision replay, runtime envelope, positive cost requirement, DB outage after known allocation, shared-slot canonical authority and preventing double-counted concurrency.

Provider audit fixes: concurrent create guard extended to native recovery; child handle/ID retained before journal commit with best-effort bounded cleanup on commit failure; uncertain file-helper timeout/cancel/transport kills or quarantines with retained ID/outcome; definite helper rejection preserves recoverable VM files. Independent read-only auditor rechecked original findings and preflight/file-rejection regressions; last cross-operation race fixed with its own test. No live E2B calls, paid resources, or fabricated cloud qualification.

Final follow-up verification: `ruff check` clean and `ruff format --check` reports all four code/test files formatted. `pytest -q tests/test_execution*.py tests/test_workspace_service.py tests/test_core.py tests/test_authority.py` **119 passed in 2.67s**. Dedicated broker suite has **18 cases**; dedicated E2B durability suite has **33 cases**. Tests use canonical SQLite transactions and real local file helpers with mocked cloud calls; PostgreSQL/cloud-native behavior remains unqualified by this follow-up. Code handed to parent for worker/tool wiring; shared-slot lifecycle obligation explicitly communicated.
